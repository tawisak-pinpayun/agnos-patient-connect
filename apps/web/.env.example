# URL ของ Socket.IO server (ต้องเป็น public URL เพราะ browser ต่อตรง)
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# URL ที่ Next.js route handler ใช้เรียก REST ฝั่ง server (server-side only)
SERVER_API_URL=http://localhost:4000
